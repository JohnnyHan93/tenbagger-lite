# Migration

This v2 build uses persist key `idt-v2`. Previous `tenbagger-lite-v3` local data is not auto-imported.

Export JSON from the old app if you still have it, then Settings → Backup is the forward path.
Universe import accepts CSV / JSON / markdown ticker tables.
